// =========================================
// BÍ CẢNH: TIẾN LÊN MIỀN NAM (ĐANG XÂY DỰNG)
// =========================================

console.log("Đã nạp Bí cảnh: Tiến Lên");

let currentTienLenRoomId = null;

function initTienLenGame() {
    // Logic chặt heo, tới trắng, bỏ lượt sẽ viết ở đây
}

function openTienLenMenu() {
    if(typeof playSfx === 'function') playSfx('click');
    showUpdatingModal();
}